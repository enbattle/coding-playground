/** Shown in both the Settings modal and the first-load welcome modal — kept as one source so the
 * two can't drift. */
export const SHORTCUTS: { key: string; label: string }[] = [
  { key: '⌘K', label: 'Command Palette' },
  { key: '⌘⏎', label: 'Run Code' },
  { key: '⇧⌥F', label: 'Format Document' },
  { key: 'Esc', label: 'Close dialogs' },
];
